# X (Twitter) API — required to show the latest post on the home page.
# Create a project at https://developer.x.com/ → User authentication settings → Bearer Token
# (Or: Keys and tokens → Bearer Token)
# Free tier limits apply; token must stay secret — use only in .env.local, never commit.
X_BEARER_TOKEN=
